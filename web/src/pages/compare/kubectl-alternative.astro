---
import BaseLayout from "../../layouts/BaseLayout.astro";
import pkg from "../../../../package.json";

const version = pkg.version;
const dmgUrl = `https://github.com/atilladeniz/Kubeli/releases/download/v${version}/Kubeli_${version}_aarch64.dmg`;
const appImageUrl = `https://github.com/atilladeniz/Kubeli/releases/download/v${version}/Kubeli_${version}_amd64.AppImage`;

const comparisonPoints = [
  {
    category: "Interface",
    kubeli: "Visual GUI",
    kubectl: "Command line",
    winner: "kubeli",
    detail: "Point-and-click vs. memorizing commands and flags."
  },
  {
    category: "Learning Curve",
    kubeli: "Intuitive",
    kubectl: "Steep",
    winner: "kubeli",
    detail: "No need to remember kubectl get, describe, logs syntax."
  },
  {
    category: "Resource Overview",
    kubeli: "Visual diagrams",
    kubectl: "Text output",
    winner: "kubeli",
    detail: "See your cluster topology at a glance."
  },
  {
    category: "Log Analysis",
    kubeli: "AI-powered",
    kubectl: "Raw text",
    winner: "kubeli",
    detail: "Claude/OpenAI helps identify errors and suggest fixes."
  },
  {
    category: "Multi-Cluster",
    kubeli: "Switch with click",
    kubectl: "Context switching",
    winner: "kubeli",
    detail: "No more kubectl config use-context."
  },
  {
    category: "Port Forwarding",
    kubeli: "Visual management",
    kubectl: "Manual commands",
    winner: "kubeli",
    detail: "One-click port forwards with browser integration."
  },
  {
    category: "Terminal Access",
    kubeli: "Built-in",
    kubectl: "Native",
    winner: "tie",
    detail: "Both provide shell access to pods."
  },
  {
    category: "Scripting",
    kubeli: "GUI focused",
    kubectl: "Full scripting",
    winner: "kubectl",
    detail: "kubectl is better for automation and scripts."
  },
  {
    category: "Price",
    kubeli: "Free (MIT)",
    kubectl: "Free",
    winner: "tie",
    detail: "Both are free and open source."
  }
];
---

<BaseLayout
  title="Kubeli vs kubectl - Visual Alternative to CLI"
  description="Looking for a kubectl alternative with a visual interface? Kubeli is a native Kubernetes GUI for macOS, Windows & Linux with AI-powered analysis. No commands to memorize."
  activeNav=""
  canonicalPath="/compare/kubectl-alternative"
>
  <!-- Hero -->
  <section class="mx-auto max-w-6xl px-4 sm:px-8 py-12 sm:py-20">
    <div class="text-center">
      <span class="inline-flex items-center rounded-full bg-cyan-50 px-3 py-1 text-xs font-medium text-cyan-700 mb-6">
        Comparison
      </span>
      <h1 class="text-3xl sm:text-4xl md:text-5xl font-bold max-w-4xl mx-auto leading-tight">
        Looking for a <span class="text-cyan-600">kubectl Alternative</span>?
      </h1>
      <p class="text-neutral-500 max-w-2xl mx-auto text-lg mt-6">
        Love Kubernetes but tired of memorizing commands? Kubeli gives you a visual interface
        with all the power of kubectl - plus AI-powered analysis.
      </p>

      <div class="mt-10">
        <a
          href={dmgUrl}
          class="inline-flex items-center gap-2 rounded-full bg-black px-8 py-3 text-lg font-medium text-white hover:bg-neutral-800 transition-colors"
        >
          Try Kubeli Free
        </a>
      </div>
    </div>
  </section>

  <!-- Key Differences -->
  <section class="bg-neutral-50 py-16 sm:py-20">
    <div class="mx-auto max-w-6xl px-4 sm:px-8">
      <h2 class="text-2xl font-bold text-center mb-4">
        kubectl commands → Kubeli clicks
      </h2>
      <p class="text-neutral-500 text-center mb-12 max-w-xl mx-auto">
        Common kubectl workflows become simple visual actions.
      </p>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 max-w-3xl mx-auto">
        <div class="bg-white p-6 rounded-xl border border-neutral-200">
          <div class="font-mono text-sm text-neutral-500 mb-2">kubectl</div>
          <code class="text-sm bg-neutral-100 px-2 py-1 rounded">kubectl get pods -n default</code>
        </div>
        <div class="bg-white p-6 rounded-xl border border-cyan-200 border-2">
          <div class="font-mono text-sm text-cyan-600 mb-2">Kubeli</div>
          <span class="text-sm">Click namespace → See all pods</span>
        </div>
        <div class="bg-white p-6 rounded-xl border border-neutral-200">
          <div class="font-mono text-sm text-neutral-500 mb-2">kubectl</div>
          <code class="text-sm bg-neutral-100 px-2 py-1 rounded">kubectl logs -f pod-name</code>
        </div>
        <div class="bg-white p-6 rounded-xl border border-cyan-200 border-2">
          <div class="font-mono text-sm text-cyan-600 mb-2">Kubeli</div>
          <span class="text-sm">Click pod → Live logs + AI analysis</span>
        </div>
        <div class="bg-white p-6 rounded-xl border border-neutral-200">
          <div class="font-mono text-sm text-neutral-500 mb-2">kubectl</div>
          <code class="text-sm bg-neutral-100 px-2 py-1 rounded">kubectl port-forward svc/name 8080:80</code>
        </div>
        <div class="bg-white p-6 rounded-xl border border-cyan-200 border-2">
          <div class="font-mono text-sm text-cyan-600 mb-2">Kubeli</div>
          <span class="text-sm">Click service → One-click forward</span>
        </div>
        <div class="bg-white p-6 rounded-xl border border-neutral-200">
          <div class="font-mono text-sm text-neutral-500 mb-2">kubectl</div>
          <code class="text-sm bg-neutral-100 px-2 py-1 rounded">kubectl config use-context prod</code>
        </div>
        <div class="bg-white p-6 rounded-xl border border-cyan-200 border-2">
          <div class="font-mono text-sm text-cyan-600 mb-2">Kubeli</div>
          <span class="text-sm">Click cluster in sidebar</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Comparison Table -->
  <section class="py-16 sm:py-24">
    <div class="mx-auto max-w-4xl px-4 sm:px-8">
      <h2 class="text-2xl sm:text-3xl font-bold text-center mb-12">
        Feature Comparison
      </h2>

      <div class="space-y-4">
        {comparisonPoints.map((point) => (
          <div class="bg-white border border-neutral-200 rounded-xl overflow-hidden">
            <div class="grid grid-cols-3 gap-4 p-4 items-center">
              <div>
                <div class="font-medium">{point.category}</div>
                <div class="text-xs text-neutral-400 mt-1">{point.detail}</div>
              </div>
              <div class={`text-center py-2 rounded-lg ${point.winner === 'kubeli' ? 'bg-cyan-50 text-cyan-700 font-medium' : ''}`}>
                {point.kubeli}
              </div>
              <div class={`text-center py-2 rounded-lg ${point.winner === 'kubectl' ? 'bg-neutral-100 text-neutral-700 font-medium' : 'text-neutral-500'}`}>
                {point.kubectl}
              </div>
            </div>
          </div>
        ))}
      </div>

      <p class="text-center text-neutral-400 text-sm mt-8">
        Note: Kubeli complements kubectl - you can still use CLI when needed.
      </p>
    </div>
  </section>

  <!-- CTA -->
  <section class="bg-neutral-900 text-white py-16 sm:py-24">
    <div class="mx-auto max-w-3xl px-4 sm:px-8 text-center">
      <h2 class="text-2xl sm:text-3xl font-bold">Ready to see your clusters visually?</h2>
      <p class="text-neutral-400 mt-4">
        Download Kubeli and experience Kubernetes management without memorizing commands.
      </p>
      <div class="mt-8 flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href={dmgUrl}
          class="inline-flex items-center gap-2 rounded-full bg-white text-black px-8 py-3 text-lg font-medium hover:bg-neutral-100 transition-colors"
        >
          Download for macOS
        </a>
        <a
          href={appImageUrl}
          class="inline-flex items-center gap-2 rounded-full border border-neutral-500 text-white px-8 py-3 text-lg font-medium hover:bg-neutral-800 transition-colors"
        >
          Download for Linux
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
